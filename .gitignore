# Runtime data — never commit business data
data/
# Amazon Business Report / inventory exports dropped in the repo root (real sales data)
BusinessReport*.csv
/*.csv
# Self-provisioned portable Node runtime
runtime/
node_modules/
*.log
.DS_Store

# Built frontend (the Dockerfile rebuilds this on deploy — no need to store it)
web/dist/
# Stray import/export files accidentally dropped in the project root
/*.xls
/*.xlsx
